services:
  - type: web
    name: ai-shelf-life
    env: node
    plan: free
    region: frankfurt
    buildCommand: "npm ci"
    startCommand: "node server_safe.js"
    envVars:
      - key: SECRET
        value: mrcina_one_secret_prod_CHANGE_ME
      - key: ADMIN_KEY
        generateValue: true
      - key: NODE_ENV
        value: production
    healthCheckPath: "/"
    autoDeploy: true
